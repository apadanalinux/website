services:
  website:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/apadanalinux/website:latest
    container_name: apadana-website
    restart: unless-stopped
    ports:
      - "80:80"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:80/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
